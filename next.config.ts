import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['via.placeholder.com','assets.aceternity.com','image.tmdb.org',"images.pexels.com","images.unsplash.com"], 
  },
};

export default nextConfig;
